import type{ReactNode}from'react';import{monthLabel}from'../../utils/format';
export type View='overview'|'insights'|'vendors'|'safety'|'shifts'|'ask'|'reports';
const nav:Array<[View,string,string]>=[['overview','Overview','⌂'],['insights','AI Insights','✦'],['vendors','Vendors','▦'],['safety','Safety','◇'],['shifts','Shift Readiness','◷'],['ask','Ask Pulse','◎'],['reports','Reports','▤']];
export function Shell({view,setView,month,setMonth,health,children}:{view:View;setView:(v:View)=>void;month:string;setMonth:(m:string)=>void;health:'healthy'|'warning';children:ReactNode}){return <div className="min-h-screen"><aside className="fixed inset-y-0 left-0 z-20 w-60 border-r border-slate-200 bg-white"><div className="flex h-[72px] items-center gap-3 border-b border-slate-200 px-5"><div className="grid h-9 w-9 place-items-center rounded-lg bg-blue-600 font-bold text-white">M</div><div><div className="text-sm font-semibold text-navy">MoveInSync <span className="text-blue-600">Pulse</span></div><div className="text-[10px] uppercase tracking-wider text-slate-500">Mobility Intelligence</div></div></div><nav className="space-y-1 p-3" aria-label="Primary navigation">{nav.map(([id,label,icon])=><button key={id} onClick={()=>setView(id)} className={`flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-left text-sm font-medium ${view===id?'bg-blue-50 text-blue-700':'text-slate-600 hover:bg-slate-50 hover:text-navy'}`}><span className="w-5 text-center text-base">{icon}</span>{label}</button>)}</nav><div className="absolute bottom-5 left-4 right-4 rounded-lg border border-slate-200 bg-slate-50 p-3"><div className="eyebrow">Data window</div><div className="mt-1 font-semibold">May–July 2026</div><div className="mt-1 text-xs text-slate-500">Deterministic analytics</div></div></aside><div className="ml-60"><header className="sticky top-0 z-10 flex h-[72px] items-center justify-between border-b border-slate-200 bg-white/95 px-8 backdrop-blur"><div><div className="font-semibold text-navy">Autonomous Mobility Intelligence</div><div className="mt-0.5 text-xs text-slate-500">Operational signals, evidence, and action</div></div><div className="flex items-center gap-4"><label className="flex items-center gap-2 text-xs font-medium text-slate-600">Reporting month<select aria-label="Reporting month" value={month} onChange={e=>setMonth(e.target.value)} className="rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm font-semibold text-navy"><option value="2026-05">May 2026</option><option value="2026-06">June 2026</option><option value="2026-07">July 2026</option></select></label><div className={`flex items-center gap-2 rounded-full px-3 py-2 text-xs font-semibold ${health==='healthy'?'bg-emerald-50 text-emerald-700':'bg-amber-50 text-amber-700'}`}><span className={`h-2 w-2 rounded-full ${health==='healthy'?'bg-emerald-500':'bg-amber-500'}`}/>{health==='healthy'?'Dataset healthy':'Review data quality'}</div></div></header><main className="mx-auto max-w-[1560px] p-8"><div className="mb-1 text-xs font-medium text-slate-500">{monthLabel(month)}</div>{children}</main></div></div>}
