import type{DataQuality}from'../../api/types';
export function DataHealth({data}:{data:DataQuality}){const prep=data.preprocessing as{rows_rejected_total?:number};const billZero=((data.preprocessing as{sources?:Array<{dataset:string;zero_distance_count:number}>}).sources||[]).find(s=>s.dataset==='bills')?.zero_distance_count;return <section className="card p-5"><div className="flex items-center justify-between"><div><div className="eyebrow">Trust layer</div><h2 className="section-title mt-1">Dataset Health</h2></div><span className="rounded-full bg-amber-50 px-3 py-1 text-xs font-semibold text-amber-700">Usable with caveats</span></div><div className="mt-4 grid grid-cols-2 gap-2 text-sm"><Status ok label="Join IDs normalized"/><Status ok={prep.rows_rejected_total===0} label="All source rows preserved"/><Status ok label="Monthly schema reconciled"/><Status label={(billZero?.toLocaleString()||'Known')+' zero-distance bills'}/><Status label="Invalid severity values present"/><Status label="Marshal zero semantics uncertain"/></div></section>}
function Status({ok=false,label}:{ok?:boolean;label:string}){return <div className="flex items-center gap-2 rounded-lg bg-slate-50 px-3 py-2"><span className={ok?'text-emerald-600':'text-amber-600'}>{ok?'✓':'⚠'}</span><span className="text-xs text-slate-700">{label}</span></div>}
